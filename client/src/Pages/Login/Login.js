import React, { useState } from 'react' 
import LoginForm from '../../components/LoginForm'

const LoginPage = _ => {
  return(
    <div>
      <LoginForm></LoginForm>
    </div>
  )
}

export default LoginPage